# 📦 依赖库安装完整指南

**文档版本**：v1.0
**更新日期**：2025-01-01
**适用系统**：Windows 10/11、macOS、Linux

---

## 📋 目录

1. [快速安装](#1-快速安装)
2. [核心依赖详解](#2-核心依赖详解)
3. [可选依赖说明](#3-可选依赖说明)
4. [TA-Lib 的问题](#4-ta-lib-的问题)
5. [安装验证](#5-安装验证)
6. [常见问题](#6-常见问题)

---

## 1. 快速安装

### 1.1 最小安装（推荐）

**只安装必需的核心依赖**：

```bash
pip install polars duckdb pandas matplotlib psutil numpy
```

**预计安装时间**：2-3 分钟
**占用空间**：约 500 MB

---

### 1.2 使用 requirements.txt 安装

**Step 1：进入项目目录**
```bash
cd D:\软件工程实训作业\project
```

**Step 2：安装依赖**
```bash
pip install -r requirements.txt
```

**Step 3：验证安装**
```bash
python -c "import polars, duckdb, pandas; print('✓ 安装成功')"
```

---

### 1.3 完整安装（包含可选依赖）

```bash
# 核心依赖
pip install polars duckdb pandas matplotlib psutil numpy

# 可选依赖（推荐）
pip install pyarrow tqdm seaborn

# 开发工具（可选）
pip install jupyter pytest
```

---

## 2. 核心依赖详解

### 2.1 Polars（高性能数据处理）

#### **作用**
- 本项目的**核心库**
- 提供比 Pandas 快 50-80 倍的数据处理能力
- 支持多线程并行计算

#### **安装**
```bash
pip install polars>=1.36.1
```

#### **验证**
```python
import polars as pl
print(f"Polars 版本: {pl.__version__}")

# 测试功能
df = pl.DataFrame({'A': [1, 2, 3], 'B': [4, 5, 6]})
print(df)
```

#### **期望输出**
```
Polars 版本: 1.36.1
shape: (3, 2)
┌─────┬─────┐
│ A   ┆ B   │
│ --- ┆ --- │
│ i64 ┆ i64 │
╞═════╪═════╡
│ 1   ┆ 4   │
│ 2   ┆ 5   │
│ 3   ┆ 6   │
└─────┴─────┘
```

#### **常见问题**
- **Q**: 安装后 import polars 报错？
- **A**: 确认 Python 版本 >= 3.8

---

### 2.2 DuckDB（嵌入式数据库）

#### **作用**
- 存储股票数据（2.1GB CSV → 622MB 数据库）
- 提供高效的列式查询
- 压缩比达到 3.3x

#### **安装**
```bash
pip install duckdb>=1.4.3
```

#### **验证**
```python
import duckdb

# 创建内存数据库
conn = duckdb.connect(':memory:')
result = conn.execute("SELECT 42 as answer").fetchone()
print(f"DuckDB 测试: {result[0]}")
conn.close()
```

#### **期望输出**
```
DuckDB 测试: 42
```

---

### 2.3 Pandas（性能对比基准）

#### **作用**
- 作为性能对比的**基准线**
- 验证 Polars 计算结果的正确性
- 提供成熟的数据处理功能

#### **安装**
```bash
pip install pandas>=2.3.3
```

#### **验证**
```python
import pandas as pd
print(f"Pandas 版本: {pd.__version__}")

df = pd.DataFrame({'A': [1, 2, 3], 'B': [4, 5, 6]})
print(df)
```

---

### 2.4 Matplotlib（图表生成）

#### **作用**
- 生成性能对比图表
- 可视化技术指标
- 生成项目报告图表

#### **安装**
```bash
pip install matplotlib>=3.10.8
```

#### **验证**
```python
import matplotlib.pyplot as plt

# 测试绘图
plt.plot([1, 2, 3], [4, 5, 6])
plt.title('测试图表')
plt.savefig('test.png')
print("✓ 图表生成成功: test.png")
```

---

### 2.5 psutil（系统监控）

#### **作用**
- 监控内存使用情况
- 监控 CPU 占用率
- 用于性能测试报告

#### **安装**
```bash
pip install psutil>=7.2.1
```

#### **验证**
```python
import psutil
import os

process = psutil.Process(os.getpid())
memory_mb = process.memory_info().rss / 1024 / 1024
print(f"当前进程内存: {memory_mb:.2f} MB")
```

---

### 2.6 NumPy（数值计算）

#### **作用**
- 提供数值计算基础
- Pandas 和 Matplotlib 的依赖
- 用于数学运算

#### **安装**
```bash
pip install numpy>=2.4.0
```

#### **验证**
```python
import numpy as np
print(f"NumPy 版本: {np.__version__}")

arr = np.array([1, 2, 3, 4, 5])
print(f"平均值: {arr.mean()}")
```

---

## 3. 可选依赖说明

### 3.1 PyArrow（推荐）

#### **为什么需要？**
- 提供 **Parquet 格式** 支持（比 CSV 更高效）
- Polars 的某些功能需要
- 提高数据导入导出性能

#### **安装**
```bash
pip install pyarrow>=19.0.0
```

#### **使用示例**
```python
import polars as pl

# 读取 Parquet 文件（比 CSV 快 5-10 倍）
df = pl.read_parquet('data.parquet')

# 保存为 Parquet（文件更小）
df.write_parquet('output.parquet')
```

---

### 3.2 tqdm（进度条）

#### **为什么需要？**
- 显示长时间操作的进度
- 提升用户体验

#### **安装**
```bash
pip install tqdm>=4.67.0
```

#### **使用示例**
```python
from tqdm import tqdm
import time

for i in tqdm(range(100), desc="处理中"):
    time.sleep(0.01)  # 模拟处理
```

**输出**：
```
处理中: 100%|██████████| 100/100 [00:01<00:00, 99.50it/s]
```

---

### 3.3 Seaborn（增强可视化）

#### **为什么需要？**
- 更美观的图表样式
- 统计可视化功能
- 基于 Matplotlib，API 更简洁

#### **安装**
```bash
pip install seaborn>=0.13.2
```

---

## 4. TA-Lib 的问题

### 4.1 什么是 TA-Lib？

**TA-Lib**（Technical Analysis Library）是一个技术分析库，包含 150+ 个技术指标的实现。

**官网**：https://ta-lib.org/

---

### 4.2 为什么本项目不用 TA-Lib？

#### **原因 1：与项目目标冲突** 🎯

```
项目目标：
✓ 使用 Polars 实现高性能计算
✓ 展示 Polars vs Pandas 的性能差异
✓ 学习如何实现技术指标

TA-Lib 的问题：
✗ 主要为 Pandas 设计
✗ 不支持 Polars DataFrame
✗ 无法展示 Polars 的性能优势
```

#### **原因 2：安装复杂** 😰

**Windows 安装步骤**：
```bash
# 1. 下载 TA-Lib C 库（.whl 文件）
# 从：https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib

# 2. 安装 Visual Studio Build Tools（可能需要）
# 下载：https://visualstudio.microsoft.com/downloads/

# 3. 手动安装 .whl 文件
pip install TA_Lib‑0.4.XX‑cpXX‑cpXX‑win_amd64.whl

# 4. 配置环境变量（可能需要）
```

**对比**：
```bash
# 安装 Polars（简单）
pip install polars       # ✓ 一行命令搞定

# 安装 TA-Lib（复杂）
# 需要 4-5 个步骤  # ✗ 容易出错
```

#### **原因 3：失去学习机会** 📚

```python
# 使用 TA-Lib（黑盒）
import talib
rsi = talib.RSI(prices, timeperiod=14)
# ✗ 不知道 RSI 是怎么计算的

# 自己实现（理解原理）
def calculate_rsi(prices, period=14):
    delta = prices.diff()
    gain = delta.where(delta > 0, 0).rolling(period).mean()
    loss = -delta.where(delta < 0, 0).rolling(period).mean()
    rs = gain / loss
    return 100 - (100 / (1 + rs))
# ✓ 完全理解 RSI 的计算过程
```

#### **原因 4：性能测试不公平** ⚖️

```
如果使用 TA-Lib：

Pandas 版本：
- 可以调用 TA-Lib（用 C 语言实现，很快）
- 性能：⭐⭐⭐⭐⭐

Polars 版本：
- 不能调用 TA-Lib（不支持 Polars）
- 只能用 Python 实现
- 性能：⭐⭐

结果：
→ Pandas 比 Polars 快（这不是我们想要的）
→ 无法展示 Polars 的真正性能
```

#### **原因 5：上届团队也没用** 📋

查看上届团队（第22组）的代码：
- ✅ 他们实现了 48 个技术指标
- ✅ 没有使用 TA-Lib
- ✅ 达到了 54 倍加速
- ✅ 证明不需要 TA-Lib 也能完成项目

---

### 4.3 什么时候可以用 TA-Lib？

**适合使用的场景**：
- ✅ 纯 Pandas 项目（不涉及 Polars）
- ✅ 快速原型开发（不关心性能）
- ✅ 指标种类多（需要 100+ 个指标）
- ✅ 生产环境（稳定性优先）

**不适合的场景**：
- ❌ 学习项目（需要理解原理）
- ❌ 性能对比项目（不公平）
- ❌ Polars 项目（不兼容）
- ❌ Windows 开发（安装复杂）

---

### 4.4 如果确实需要 TA-Lib

**Step 1：下载预编译版本**
```
访问：https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib
下载对应版本：
- Python 3.14 + Windows 64位：
  TA_Lib‑0.4.XX‑cp314‑cp314‑win_amd64.whl
```

**Step 2：安装**
```bash
pip install TA_Lib‑0.4.XX‑cp314‑cp314‑win_amd64.whl
```

**Step 3：验证**
```python
import talib
print(talib.get_functions())  # 查看所有函数
```

---

## 5. 安装验证

### 5.1 完整验证脚本

创建文件：`verify_install.py`

```python
"""
依赖库安装验证脚本
"""

def verify_installation():
    """验证所有依赖是否正确安装"""

    print("=" * 60)
    print("开始验证依赖库安装...")
    print("=" * 60)

    # 必需依赖
    required = [
        ('polars', '1.36.0'),
        ('duckdb', '1.4.0'),
        ('pandas', '2.3.0'),
        ('matplotlib', '3.10.0'),
        ('psutil', '7.2.0'),
        ('numpy', '2.4.0')
    ]

    # 可选依赖
    optional = [
        ('pyarrow', '19.0.0'),
        ('tqdm', '4.67.0'),
        ('seaborn', '0.13.0')
    ]

    success_count = 0
    total_count = len(required)

    print("\n【必需依赖】")
    for package, min_version in required:
        try:
            module = __import__(package)
            version = getattr(module, '__version__', 'unknown')
            print(f"✓ {package:15s} {version:10s} (要求 >= {min_version})")
            success_count += 1
        except ImportError:
            print(f"✗ {package:15s} 未安装！")

    print(f"\n必需依赖安装成功: {success_count}/{total_count}")

    print("\n【可选依赖】")
    for package, min_version in optional:
        try:
            module = __import__(package)
            version = getattr(module, '__version__', 'unknown')
            print(f"✓ {package:15s} {version:10s} (推荐)")
        except ImportError:
            print(f"- {package:15s} 未安装（可选）")

    print("\n" + "=" * 60)
    if success_count == total_count:
        print("✅ 所有必需依赖已正确安装！")
        return True
    else:
        print("❌ 部分必需依赖缺失，请安装后再试。")
        return False

if __name__ == "__main__":
    verify_installation()
```

**运行验证**：
```bash
python verify_install.py
```

---

### 5.2 快速验证（一行命令）

```bash
python -c "import polars, duckdb, pandas, matplotlib, psutil, numpy; print('✓ 核心依赖全部安装成功')"
```

---

## 6. 常见问题

### Q1: pip 安装速度很慢怎么办？

**A**: 使用国内镜像源

```bash
# 临时使用
pip install polars -i https://pypi.tuna.tsinghua.edu.cn/simple

# 永久设置
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

**推荐镜像源**：
- 清华：https://pypi.tuna.tsinghua.edu.cn/simple
- 阿里：https://mirrors.aliyun.com/pypi/simple
- 中科大：https://pypi.mirrors.ustc.edu.cn/simple

---

### Q2: 安装时提示权限不足？

**A**: 使用 `--user` 参数

```bash
pip install polars --user
```

或使用管理员权限：
```bash
# Windows（以管理员身份运行 cmd）
pip install polars

# Linux/macOS
sudo pip install polars
```

---

### Q3: 如何升级已安装的库？

**A**: 使用 `--upgrade` 参数

```bash
# 升级单个库
pip install --upgrade polars

# 升级所有库
pip install --upgrade -r requirements.txt
```

---

### Q4: 如何卸载不需要的库？

**A**: 使用 `pip uninstall`

```bash
pip uninstall talib
```

---

### Q5: Python 版本太低怎么办？

**A**: 升级 Python

```bash
# 检查版本
python --version

# 如果 < 3.10，需要升级
# Windows: 从 python.org 下载最新版本
# macOS: brew install python@3.14
# Linux: sudo apt install python3.14
```

---

### Q6: 多个 Python 版本如何管理？

**A**: 使用虚拟环境

```bash
# 创建虚拟环境
python -m venv project_env

# 激活虚拟环境
# Windows:
project_env\Scripts\activate

# Linux/macOS:
source project_env/bin/activate

# 安装依赖
pip install -r requirements.txt

# 退出虚拟环境
deactivate
```

---

## 📊 总结

### **推荐安装方案**

#### **方案 1：最小安装**（推荐新手）
```bash
pip install polars duckdb pandas matplotlib psutil numpy
```

#### **方案 2：推荐安装**（推荐）
```bash
pip install polars duckdb pandas matplotlib psutil numpy pyarrow tqdm
```

#### **方案 3：完整安装**（用于开发）
```bash
pip install -r requirements.txt
```

---

### **不推荐安装**

❌ **TA-Lib**（除非有特殊需求）
- 安装复杂
- 与项目目标冲突
- 不支持 Polars

---

**文档更新日期**：2025-01-01
**下次更新**：发现新的依赖需求时

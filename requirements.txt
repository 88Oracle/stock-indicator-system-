# ============================================================
# Polars + DuckDB 股票指标计算系统 - 依赖清单
# ============================================================
# 生成日期：2025-01-01
# Python 版本要求：>= 3.10
# ============================================================

# ------------------------------------------------------------
# 核心依赖（必需）
# ------------------------------------------------------------

# 数据处理
polars>=1.36.1              # 高性能数据处理库（核心）
pandas>=2.3.3               # 传统数据处理库（用于性能对比）
numpy>=2.4.0                # 数值计算基础库

# 数据库
duckdb>=1.4.3               # 嵌入式分析型数据库

# 可视化
matplotlib>=3.10.8          # 基础图表库

# 系统监控
psutil>=7.2.1               # 系统资源监控（CPU、内存）

# ------------------------------------------------------------
# 可选依赖（增强功能）
# ------------------------------------------------------------

# 高效数据格式（推荐）
pyarrow>=19.0.0             # Apache Arrow 格式支持
fastparquet>=2024.11.0      # Parquet 格式支持

# 进度显示（推荐）
tqdm>=4.67.0                # 进度条显示

# 增强可视化（可选）
seaborn>=0.13.2             # 统计可视化
plotly>=5.24.1              # 交互式图表

# 性能分析工具（可选）
line-profiler>=4.2.0        # 逐行性能分析
memory-profiler>=0.61.0     # 内存使用分析

# Jupyter 支持（可选，如果使用 Notebook）
jupyter>=1.1.1              # Jupyter Notebook
ipykernel>=6.29.5           # Python 内核

# ------------------------------------------------------------
# 不推荐的依赖
# ------------------------------------------------------------

# TA-Lib（不推荐使用）
# 原因：
# 1. 主要为 Pandas 设计，不支持 Polars
# 2. Windows 安装复杂，需要 C 编译器
# 3. 与项目目标（展示 Polars 性能）冲突
# 4. 自己实现指标可以更好地学习和理解
#
# 如果确实需要，可以手动安装：
# pip install TA-Lib
# 注意：Windows 用户需要先从这里下载预编译版本：
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib

# ------------------------------------------------------------
# 开发工具依赖（可选，用于开发阶段）
# ------------------------------------------------------------

# 代码质量
# pylint>=3.3.2             # 代码检查
# black>=24.10.0            # 代码格式化
# flake8>=7.1.1             # 代码风格检查

# 测试工具
# pytest>=8.3.4             # 单元测试框架
# pytest-cov>=6.0.0         # 测试覆盖率

# ------------------------------------------------------------
# 安装说明
# ------------------------------------------------------------

# 最小安装（只安装必需依赖）：
#   pip install -r requirements.txt --no-optional-dependencies
#
# 完整安装（安装所有依赖）：
#   pip install -r requirements.txt
#
# 升级所有依赖：
#   pip install -r requirements.txt --upgrade
#
# 安装指定依赖：
#   pip install polars duckdb pandas matplotlib psutil numpy

# ------------------------------------------------------------
# 版本锁定说明
# ------------------------------------------------------------

# 使用 >= 而不是 == 的原因：
# - 允许安装小版本更新（如 1.36.1 → 1.36.2）
# - 避免依赖冲突
# - 获取安全补丁和 bug 修复
#
# 如果需要完全锁定版本（生产环境），运行：
#   pip freeze > requirements-lock.txt
